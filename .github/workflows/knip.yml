name: knip
on:
  pull_request:

permissions:
  checks: write
  issues: write
  pull-requests: write

jobs:
  knip:
    runs-on: ubuntu-latest
    steps:
      - name: Post the knip results
        uses: codex-/knip-reporter@v2

